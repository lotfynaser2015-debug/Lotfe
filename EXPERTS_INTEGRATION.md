# دمج نظام الخبراء مع بوت المحافظ (Spot)

## الفكرة
- أنت تحدد **التايم فريم** والمحافظ من داخل البوت.
- الخبراء يصوتوا (سلسلة/حيتان، سيولة، شارت، حالة سوق، مزاج، أخبار).
- **خبير المخاطر** له فيتو مطلق: حجم صغير/متوسط فقط، رفض عند ضعف الحجم أو النطاق أو ضعف الإجماع.
- التنفيذ **يدوي فقط** من زر التأكيد بعد التقرير — مفيش أوامر تلقائية.

## مسار التحكم من البوت (مفهوم 100%)

1. القائمة الرئيسية → **🧠 تحليل الخبراء**
2. اختر التايم فريم: 15m / 1h / 4h / 1d / 1w
3. اختر المحافظ (✓) ثم **تشغيل التحليل**
4. يظهر التقرير: القرار + الأصوات + فيتو المخاطر + مصدر البيانات من MEXC Spot
5. لو القرار شراء/بيع ومرّ من المخاطر → يظهر زر **تنفيذ يدوي**
6. تأكيد ثاني → يبني إشارة `#SIGNAL` ويمررها لمحرك الإشارات الحالي على المحافظ المختارة

## البيانات الحية (MEXC Spot)

`MexcClient.get_expert_market_context()` يقرأ:
- OHLCV (حجم، اتجاه SMA، زخم، نطاق، كسر)
- Order Book (انحياز شراء/بيع)

لو فشلت القراءة → سياق محافظ يمنع أي دخول.

## الحماية
1. أقصى حجم: small أو medium فقط (مش Full عشوائي)
2. مفيش دخول في نطاق ضيق بدون كسر
3. مفيش دخول لو الأحجام ضعيفة
4. توقف بعد 3 خسائر متتالية (RiskManager)
5. `auto_execute_enabled = False` دائماً في مسار التحليل
6. التنفيذ الحقيقي يحتاج ضغط زرين (تنفيذ يدوي → نعم نفّذ)

## الملفات
- `experts.py` — 6 خبراء + خبير مخاطر
- `decision_engine.py` — ربط القرار + التقرير
- `mexc_client.py` — `get_expert_market_context`
- `bot.py` — واجهة التحكم الكاملة

## ملاحظة Spot
المحافظ تعمل على **MEXC Spot** فقط. نظام الخبراء لا يفتح عقود ولا رافعة.
